'use client';import{useEffect,useState}from'react';export default function P(){let[p,s]=useState<any[]>([]),[m,n]=useState<any[]>([]);useEffect(()=>{Promise.all(['/api/projects','/api/one?section=projects'].map(x=>fetch(x).then(x=>x.json()))).then(([a,b])=>{s(a.projects||[]);n(b.projects||[])})},[]);return <main><h1>Open projects</h1>{p.map(x=>{let y=m.find(z=>z.title===x.managementTitle);return <a key={x.path} href={x.html_url}><h2>{x.name}</h2><p>{x.managementTitle}</p><p>{y?.status}</p><p>{y?.notes}</p></a>})}</main>}
